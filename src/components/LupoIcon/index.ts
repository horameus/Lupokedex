import { BaseIcon } from './LupoIcon';
import { SearchIcon } from './icons/SearchIcon';
export type { LupoIconProps } from './types';

export const LupoIcon = Object.assign(BaseIcon, {
    SearchIcon,
});
